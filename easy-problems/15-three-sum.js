var threeSum = function (nums) {
    nums = nums.sort((a,b) => a - b);
    var len = nums.length - 1;
    var slowPointer = len;
    var fastPointer = 0;
    for(var i = slowPointer; i < len; i++ ){
        for(var j = )
    }
    console.log('nums :', nums);
};

console.log(threeSum([-1,0,1,2,-1,-4]));